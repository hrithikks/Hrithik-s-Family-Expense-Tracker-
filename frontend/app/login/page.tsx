'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { api } from '../../lib/api'; import { PasswordInput } from '../../components/password-input'; import { useToast } from '../../components/toast';
export default function Login(){const router=useRouter();const {toast}=useToast();const [email,setEmail]=useState('ravi@family.local');const [password,setPassword]=useState('ChangeMe123!');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});router.replace('/dashboard')}catch(err){toast(err instanceof Error?err.message:'Unable to sign in','error')}finally{setLoading(false)}}return <main className="min-h-screen bg-[#DDE7D7] p-5 grid place-items-center"><section className="panel w-full max-w-md p-7 shadow-soft"><p className="m-0 text-xs font-bold uppercase tracking-[.15em] text-[#166534]">Family finance</p><h1 className="mt-2 text-2xl">Hrithik's Family Ledger</h1><p className="mb-7 text-sm leading-6 text-[#567061]">Your shared household expense tracker.</p><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-bold">Email<input className="field mt-1" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="block text-sm font-bold">Password<span className="mt-1 block"><PasswordInput value={password} onChange={setPassword} placeholder="Enter your password" required/></span></label><button disabled={loading} className="btn btn-primary w-full">{loading?'Signing in...':'Sign in'}</button></form></section></main>}
