'use client';
import { useEffect,useState } from 'react'; import { api } from '../lib/api';
export function AuditLog(){const [logs,setLogs]=useState<any[]>([]);useEffect(()=>{api<{data:any[]}>('/audit-logs?limit=100').then(x=>setLogs(x.data))},[]);return <section className="panel overflow-x-auto p-5"><div className="mb-4"><p className="m-0 text-xs font-bold uppercase tracking-[.13em] text-[#166534]">Accountability</p><h3 className="m-1 text-xl">Audit log</h3></div><table className="data-table min-w-[700px] w-full"><thead><tr><th>When</th><th>Who</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{new Date(l.createdAt).toLocaleString('en-IN')}</td><td>{l.actor?`${l.actor.firstName} ${l.actor.lastName}`:'System'}</td><td>{l.action.replaceAll('_',' ')}</td><td>{l.entityType}</td><td>{l.details?JSON.stringify(l.details):'—'}</td></tr>)}</tbody></table>{!logs.length&&<p className="py-8 text-center text-sm text-[#759080]">No activity recorded yet.</p>}</section>}
