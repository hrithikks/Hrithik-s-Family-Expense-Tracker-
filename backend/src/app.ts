import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import cookieParser from 'cookie-parser'; import rateLimit from 'express-rate-limit';
import { config } from './config.js'; import { errorHandler } from './lib/errors.js';
import auth from './routes/auth.js'; import users from './routes/users.js'; import responsibilities from './routes/responsibilities.js'; import expenses from './routes/expenses.js'; import reports from './routes/reports.js'; import dashboard from './routes/dashboard.js'; import profile from './routes/profile.js'; import audit from './routes/audit.js';
export const app=express();app.use(helmet());app.use(cors({origin:config.frontendUrl,credentials:true}));app.use(express.json({limit:'1mb'}));app.use(cookieParser());app.use('/api/auth',rateLimit({windowMs:15*60*1000,max:50,standardHeaders:true,legacyHeaders:false}),auth);app.use('/api/users',users);app.use('/api/responsibilities',responsibilities);app.use('/api/expenses',expenses);app.use('/api/reports',reports);app.use('/api/dashboard',dashboard);app.use('/api/profile',profile);app.use('/api/audit-logs',audit);app.get('/health',(_req,res)=>res.json({ok:true}));app.use(errorHandler);
